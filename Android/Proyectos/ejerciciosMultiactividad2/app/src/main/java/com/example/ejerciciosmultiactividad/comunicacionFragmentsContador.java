package com.example.ejerciciosmultiactividad;


import android.annotation.SuppressLint;
import android.os.Bundle;
import android.widget.ImageButton;
import android.widget.ImageView;
import android.widget.TextView;

import androidx.appcompat.app.AppCompatActivity;

public class comunicacionFragmentsContador extends AppCompatActivity {
/*

    ImageButton tick;
    fragmentsContador fragment;
    TextView color;
    @SuppressLint("MissingInflatedId")
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_comunicacion_fragments_contador);
        tick = findViewById(R.id.u4a1idimbttick);
        color = findViewById(R.id.u4a1idtvcolor);

        //getFragmentManager().getFragments() // Esto devuelve list
        fragment = (comunicacionFragmentsContador) getSupportFragmentManager().findFragmentById(R.id.ut0401idfragment1);

        tick.setOnClickListener((View)->{
            fragment.setColor(0x77, 0xFF, 00, 0xFF);
            fragment.setText("Truco de magia con esta cadena mágica");
        });

        fragment.setColorChangeListener(
                (int c)-> color.setText("Funciona asqueroso " + String.valueOf(c))
        );
    }
}
*/

}